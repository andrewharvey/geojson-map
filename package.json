{
  "name": "geojson-map",
  "version": "1.0.0",
  "description": "Apply a map function to a GeoJSON FeatureCollection's Features",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Harvey <andrew@alantgeo.com.au>",
  "license": "ISC",
  "dependencies": {
    "geojson-stream": "^0.0.1",
    "stream-combiner": "^0.2.2",
    "through": "^2.3.8"
  }
}
